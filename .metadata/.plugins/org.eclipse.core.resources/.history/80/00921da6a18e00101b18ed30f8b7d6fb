package curso_programacao;
import java.util.Scanner;

public class Exercicio3_8 {

	public static void main(String[] args) {
				
		Scanner sc = new Scanner(System.in);
		double renda;
		double imposto;
		double aliquota1;
		double aliquota2;
		double aliquota3;
		
		System.out.println("Digite o valor da renda:");
		renda = sc.nextDouble();
		
		if(renda <= 2000.00) {
			System.out.println("Isento");
		}
		else if(renda > 2000.00 && renda <= 3000.00) {
			aliquota1 = (renda - 2000) * 0.08;
			System.out.printf("Imposto a pagar: %.2f", aliquota1);
		}
		else if(renda > 3000.00 && renda <= 4500.00) {
			aliquota1 = 80.00;
			aliquota2 = (renda - 3000) * 0.18;
			imposto = aliquota1 + aliquota2;
			System.out.printf("Imposto a pagar: %.2f", imposto);
		}
	}
}
